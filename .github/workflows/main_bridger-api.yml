name: Build and deploy Node.js app to Azure - bridger-api

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      - name: npm install, build, and test
        working-directory: ./backend
        run: |
          npm install
          npm run build --if-present

      - name: Generate Prisma Client
        working-directory: ./backend
        run: npx prisma generate

      - name: Zip artifact for deployment
        working-directory: ./backend
        run: zip -r ../release.zip . -x "node_modules/.cache/*"

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: release.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write
 
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Unzip artifact for deployment
        run: unzip release.zip

      - name: Debug — what's in the deploy workdir
        run: |
          pwd
          ls -la
          ls -la node_modules/.bin/prisma* 2>/dev/null || echo "no prisma in node_modules/.bin"
          ls -la node_modules/prisma 2>/dev/null | head -5 || echo "no node_modules/prisma"
          ls -la node_modules/@prisma 2>/dev/null | head -5 || echo "no node_modules/@prisma"
          cat package.json 2>/dev/null | head -20 || echo "no package.json"
      - name: Run Prisma migrations
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'bridger-api'
          slot-name: 'production'
          package: .
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
