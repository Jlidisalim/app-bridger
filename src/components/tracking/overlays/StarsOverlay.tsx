import React from 'react';
import { View, StyleSheet } from 'react-native';

export const StarsOverlay: React.FC = () => {
  // Placeholder for stars overlay. In the future, this could show animated stars.
  return null;
};

const styles = StyleSheet.create({});